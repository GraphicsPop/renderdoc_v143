# 最终版解读：知乎文章《魔改RenderDoc截帧PC端〈鸣潮〉》对照笔记

## 结论

这篇文章的核心目标，不是普通的兼容性修改，而是两件事：

1. 把 `RenderDoc` 整套工具链的可识别特征改名、去标识化。
2. 调整注入相关实现，减少被目标程序或其保护组件识别的机会。

换句话说，文章并不是在讨论“怎样增强 RenderDoc 的正常抓帧能力”，而是在讨论“目标程序如何识别 RenderDoc，以及作者认为哪些位置需要改名或替换，才能降低被识别概率”。

## 文章原文的核心判断

根据保存下来的网页原文，文章前言明确表达了下面几点：

- 作者希望在 PC 环境下截取游戏的 Shader。
- 目标程序会识别现成的 RenderDoc。
- 即使不从 RenderDoc 启动目标程序，只要机器上存在打包好的 RenderDoc，也可能触发识别。
- 除了改 RenderDoc 自身的特征，还需要处理和注入链路有关的部分。

因此，后续所有修改点都围绕一个共同目标展开：减少程序、模块、符号、路径、共享对象、日志或注入行为中出现的 `RenderDoc` 明显痕迹。

## 为什么文章会修改这些地方

### 1. Replay Marker 导出符号

文章最先提到的是 `renderdoc/api/replay/renderdoc_replay.h` 里的 replay marker 符号。

作者认为这里重要，是因为：

- 这是一个非常显眼的导出标识。
- 它参与区分“当前进程是 RenderDoc 自己的回放/UI 进程”还是“被注入的目标程序”。
- 如果整套程序都改名了，但这个核心导出符号还保留原名，那么内部模块之间可能识别不一致。

文章里还特别说明了一个风险：如果 DLL 期待的是改名后的 marker，但 EXE 仍导出原始 marker，那么模块之间会误判身份，进一步导致错误的 GPU hook 行为，甚至 UI 崩溃。

所以，作者把这个点视为“最核心的改名点”，原因不是它功能复杂，而是它处在整套程序自我识别链路的中心。

### 2. `renderdoccmd.exe` 等可执行文件路径硬编码

文章接着列出了 `renderdoc/os/win32/win32_process.cpp` 中多处 `renderdoccmd.exe` 路径替换。

作者会改这些位置，原因很直接：

- 有些子进程启动逻辑不是动态扫描出来的，而是源码里硬编码了文件名。
- 如果最终产物已经重命名，但代码里仍然按旧名字去拼路径、找程序、拉起子进程，那么流程会断掉。
- 即使功能不崩，也会把原始名称暴露在命令行、路径、进程树中。

所以这类修改的本质，是让“改名后的工具链仍然能找到改名后的自己”，同时减少原名称出现在系统可见位置上的机会。

### 3. `renderdocshim*.dll` 的路径与命名

文章也集中修改了 `renderdocshim32.dll`、`renderdocshim64.dll` 一类名称。

作者认为这里需要改，是因为：

- `shim` 本身就是注入链路中的关键 DLL。
- 这类 DLL 名称很容易成为检测规则的目标。
- 如果主程序改名了，但用于注入或桥接的 shim DLL 仍保留原名，那么路径、模块枚举、加载记录中还是会出现明显特征。

因此，shim 的修改和前面的 EXE 改名是同一类问题：不是单纯“美化命名”，而是为了让整条启动和注入链路在名字层面保持一致。

### 4. 进程白名单或自排除逻辑

文章提到 `renderdoc/os/win32/sys_win32_hooks.cpp` 里和 `renderdoccmd.exe`、`qrenderdoc.exe` 相关的判断，要同步改名。

这样改的原因通常是：

- RenderDoc 自己需要知道“哪些进程是自己人”，避免把 hook 或注入逻辑错误地施加到自身工具进程上。
- 如果主程序和子程序都改名了，但这些白名单判断还保留原始名称，那么工具可能认不出“改名后的自己”。
- 最坏情况下，会把自己的 UI、辅助进程、命令行工具错当成普通目标进程处理。

文章把这一块称作“避免注入到 RenderTest 自身”，本质上就是在维持改名前后的自识别一致性。

### 5. 崩溃处理相关名称

文章列出了 `renderdoccmd/renderdoccmd_win32.cpp`、`core/crash_handler.h` 中若干崩溃处理相关名称，比如：

- 命名对象名
- 模块名判断
- dump 路径
- breakpad server 名称

作者会改这些地方，是因为：

- 这些字符串往往会出现在系统对象、异常处理组件、日志目录、转储目录中。
- 即使抓帧主流程已经改名，崩溃处理链上残留的 `RenderDoc` 字样仍然会暴露工具身份。
- 如果目标程序或保护模块会枚举这类对象、目录或模块名，那么这些残留标识也会成为检测面。

也就是说，文章不是把 crash handler 当作功能核心来改，而是把它视为一组“容易泄露原始品牌名的外围痕迹”。

### 6. 文件路径、注册表、日志目录

文章中 `renderdoc/os/win32/win32_stringio.cpp` 的修改，集中在：

- `qrenderdoc.exe` 的路径
- 文件关联或图标相关的注册表路径
- 日志目录名

这些地方会被改，是因为它们属于非常典型的系统层痕迹：

- 路径和注册表项经常带有产品名。
- 默认日志目录和关联项常常是静态、固定、可检索的。
- 如果只是修改 EXE/DLL 名称，而这些系统层面的字符串还保留 `RenderDoc`，那么“去标识化”并不完整。

所以这部分反映的是作者的整体思路：不仅改模块名，还要改系统落地痕迹。

### 7. OpenGL 窗口类名、共享内存名、资源字符串

文章后面还提到了：

- OpenGL 窗口类名
- 全局 Hook 共享内存名称
- `data/renderdoc.rc` 里的资源字符串
- `qrenderdoc` 工程中的产品名、日志前缀、版本输出、工程名等

作者改这些点，原因仍然一致：

- 窗口类名可以被窗口枚举识别。
- 共享内存或其他命名内核对象可以被系统层枚举或定向检测。
- 资源字符串和版本信息会出现在文件属性中。
- UI 程序日志、工程名、产品名会在多处显式暴露原始品牌名。

这些地方单独看都不复杂，但放在一起构成了“静态特征面”。文章的思路是尽量把这部分也一起改掉，避免留下明显的字符串证据。

### 8. 注入方式调整与 `SetThreadContext`

文章最后提到 CrashSight 检测，并给出一句方向性说明：可以在 hook 时采用 `SetThreadContext` 注入。

这里只从“作者为什么会提这个点”做解读，不讨论实现。

作者会提这个方向，说明他的判断是：

- 单纯改字符串特征还不够。
- 目标程序或其保护组件不只是看名字，也可能看注入链路、线程行为、远程执行方式等动态特征。
- 因此，除了“去标识化”，还要减少原本那套注入流程在行为层面的可识别性。

所以，文章结构其实很清晰：

- 前半段在改“静态特征”。
- 最后这部分是在碰“动态行为特征”。

## 文章整体思路可以怎么概括

如果把整篇文章压缩成一句话，它的逻辑是：

> 先把所有容易暴露 `RenderDoc` 身份的名字、路径、对象、资源、日志、共享内存、工程名统一改掉，再处理注入链路本身可能暴露的行为特征。

这也是为什么文章会列那么多看起来零散的表格。它们不是随机修改，而是在覆盖几类不同的检测面：

- 二进制和导出符号特征
- 可执行文件和 DLL 名称特征
- 路径、注册表、日志、资源等静态落地痕迹
- 自身进程识别和自排除逻辑
- 注入行为与线程上下文相关的动态痕迹

## 和当前仓库对照时应该怎么理解

对照当前 `renderdoc_v143` 仓库时，最重要的不是死盯文章里的行号，而是理解“为什么这一类地方会被作者挑出来改”。

因为：

- 文章保存时基于的源码版本和你现在手头版本可能不完全一致。
- 行号漂移是正常现象。
- 真正稳定的是“修改类型”和“字符串/对象名称所属的职责”。

所以，对照时更适合用下面这套思路：

1. 先看该条目属于哪一类特征面。
2. 再看当前版本里对应的字符串、对象名或判断逻辑是否还存在。
3. 最后再理解它在整条启动、注入、回放、UI、自识别链路中扮演什么角色。

## 本笔记的边界

这份笔记只整理了“最终版解读”：

- 解释文章的目标是什么。
- 解释文章为什么会修改那些位置。
- 帮助后续回看时快速理解每类修改的意图。

这份笔记不包含具体实施步骤，也不包含逐条修改指南。

## 对照表：文章想替换的是当前哪一层职责

| 文章提到的改动方向 | 当前代码中对应的职责层 | 现有代码大致位置 | 文章想替换/调整的核心点 | 白话理解 |
| --- | --- | --- | --- | --- |
| `Replay Marker` 符号 | 模块自识别层 | `renderdoc/api/replay/renderdoc_replay.h` | 让各模块对“自己人是谁”保持一致 | 先统一内部暗号，别让 UI、DLL、目标进程互相认错 |
| `renderdoccmd.exe` 路径硬编码 | 子进程定位/辅助进程启动层 | `renderdoc/os/win32/win32_process.cpp` | 让辅助进程查找链路跟着改名后的产物走 | 工具要先找得到自己家的 helper |
| `renderdocshim*.dll` 路径硬编码 | 注入桥接层 | `renderdoc/os/win32/win32_process.cpp` | 让 shim 的加载路径与整套命名一致 | 负责“搭桥”的 DLL 也得能被正确找到 |
| 进程白名单/自排除逻辑 | 递归注入控制层 | `renderdoc/os/win32/sys_win32_hooks.cpp` | 避免工具把改名后的自己再次当成目标处理 | 别把自己人误伤了 |
| 命名内核对象、dump 路径、crash server 名称 | 崩溃处理协同层 | `renderdoc/core/crash_handler.h`、`renderdoccmd/renderdoccmd_win32.cpp` | 减少 crash 流程里暴露出的静态名称痕迹 | 程序崩溃时也别把原始名字漏出去 |
| 注册表、日志、文件关联、默认路径 | 系统落地痕迹层 | `renderdoc/os/win32/win32_stringio.cpp` | 清理安装后、运行后可被系统层看到的名称 | 不只是程序名，连痕迹也想一起改 |
| OpenGL 窗口类名 | 辅助窗口标识层 | 当前仓库相关 GL 平台代码 | 改掉窗口级别的固定标识 | 连内部小窗口的“门牌号”也不想暴露 |
| 全局 Hook 共享内存名称 | 跨进程共享状态层 | `renderdocshim/renderdocshim.h` | 改掉全局共享对象名字 | 多进程之间对接用的“共享公告板”也被纳入特征面 |
| Qt UI 层产品名、日志前缀、工程名 | 界面展示层 | `qrenderdoc/Code/qrenderdoc.cpp`、`qrenderdoc/renderdocui_stub.cpp`、`qrenderdoc/renderdocui_stub.vcxproj` | 让 UI 对外展示与整套改名保持一致 | 用户看得到的壳子也要同步 |
| `CreateRemoteThread` 这类现有注入路径 | 动态执行层 | `renderdoc/os/win32/win32_process.cpp` | 文章最后想动的其实是这层 | 前面改的是“名字”，这里碰的是“行为方式” |
| `AppInit_DLLs` 全局 hook 路线 | 系统级注入入口层 | `renderdoc/os/win32/win32_process.cpp` | 文章提到的 CrashSight 方向，实质是在质疑现有动态入口是否太显眼 | 不只是叫什么，还包括“怎么进去” |

一句话概括这张表：

- 文章前半段主要在改“静态特征层”。
- 文章最后提到的 CrashSight 那句，开始碰的是“动态执行层/注入入口层”。
